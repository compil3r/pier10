import { createTheme } from '@mui/material/styles';

// Definindo o tema com a fonte Comfortaa
const theme = createTheme({
  typography: {
    fontFamily: 'Comfortaa, Arial, sans-serif',
  },
});

export default theme;
